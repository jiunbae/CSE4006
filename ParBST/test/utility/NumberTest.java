package utility;

import org.junit.Test;

import static org.junit.Assert.*;

public class NumberTest {
    static int[] tests = { 1, 3, 1, 7, 2 , 4};

    @Test
    public void eval() throws Exception {
        for (int i : tests) {
            for (int j : tests) {
//                assertEquals(Number.eval(i, j, Number.Operator.LT), i < j);
//                assertEquals(Number.eval(i, j, Number.Operator.GT), i > j);
//                assertEquals(Number.eval(i, j, Number.Operator.LTE), i >= j);
//                assertEquals(Number.eval(i, j, Number.Operator.GTE), i <= j);
//                assertEquals(Number.eval(i, j, Number.Operator.EQ), i == j);
//                assertEquals(Number.eval(i, j, Number.Operator.NEQ), i != j);
            }
        }
    }
}